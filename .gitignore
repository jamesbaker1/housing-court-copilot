# Dependencies / build artifacts
node_modules/
.next/
.open-next/
.wrangler/
*.tsbuildinfo

# Local data — NEVER commit (may contain tenant PII from the file-store fallback)
.data/
web/.data/

# Secrets (only .env.example is committed)
.env
.env.local
.env*.local
# Wrangler local secrets — NEVER commit (ANTHROPIC/TWILIO/CF_ACCESS/TURNSTILE/CASE_PII_KEY)
.dev.vars
.dev.vars.*
*.dev.vars

# OS / editor
.DS_Store
*.log
