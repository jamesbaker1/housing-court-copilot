# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env / secrets — never commit
.env
.env.local
.env*.local
# Wrangler local secrets — NEVER commit (ANTHROPIC/TWILIO/CF_ACCESS/TURNSTILE/CASE_PII_KEY)
.dev.vars
.dev.vars.*
*.dev.vars

# Local data — may contain tenant PII from the file-store fallback
.data/

# typescript
*.tsbuildinfo
cloudflare-env.d.ts

# cloudflare / opennext build + local D1 (miniflare) state
/.open-next/
/.wrangler/

# editor
.vscode/
.idea/
